---
title: Vat API Overview
description: "The Vat API allows you to calculate the value added tax (VAT) on specific goods, and satisfy other VAT requirements in your app"
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";

The VAT API can be used to calculate Value added tax (VAT) on goods, validate a company's VAT number, and get the latest VAT rates for product categories in a country.
In `abstract-python`, the `VatAPI` class is used to communicate with this API.

## Endpoints

<CardGrid>

    <LinkCard
        title="Validate VAT"
        href="/vat/validate/"
        description="Validate VAT numbers in your app."
    />

    <LinkCard
        title="Calculate VAT"
        href="/vat/calculate/"
        description="Calculate the VAT fee on goods in a country."
    />

    <LinkCard
        title="VAT Categories"
        href="/vat/vat_categories/"
        description="Get a detailed `JSON` object of product categories and their VAT fee in each country."
    />

</CardGrid>

## Supported currency codes

AbstractAPI supports ISO 4217 currency codes for the VAT API which can be found [here](https://docs.abstractapi.com/vat-validation#country-codes)

### Get currency codes

With `abstract-python` you can get the supported country codes as a Python List. See usage below:

```python
from abstract_python import VAT_SUPPORTED_COUNTRIES

print(VAT_SUPPORTED_COUNTRIES)
```

Below is the result:

```python
>>> [
    "AT", "BE", "BG", "CY", "CZ", "DE", "DK", "EE", "EL", "ES", "FI", "FR", "HR",
    "HU", "IR", "IT", "LT", "LU", "LV",
    "MT", "NL", "PL", "PT", "RO", "SE", "SI", "SK", "XI",
    ]
```

### Use case

If your app receives country codes from users, you may need to validate these inputs before querying the `VatAPI` class.

See usage below:

```python
from abstract_python import VAT_SUPPORTED_COUNTRIES

country_code = input("Enter a country code")

if country code in VAT_SUPPORTED_COUNTRIES:
    # ...
else:
    return f"{country_code} is not a supported country code"
```
